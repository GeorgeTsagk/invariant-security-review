interface:
  display_name: "Invariant Security Review"
  short_description: "Structured invariant-driven code security scans"
  default_prompt: "Use $invariant-security-review to inspect the target repository, build and maintain the review catalog, challenge gray areas, and have me verify each detected invariant before an evidence-backed security review."
policy:
  allow_implicit_invocation: true
