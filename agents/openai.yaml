interface:
  display_name: "Invariant Security Review"
  short_description: "Structured invariant-driven code security scans"
  default_prompt: "Use $invariant-security-review to register this pass, recover prior invariant work, inspect the target, have me verify invariants, then stop for prioritized scan selection."
policy:
  allow_implicit_invocation: true
