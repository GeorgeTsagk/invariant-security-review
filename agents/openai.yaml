interface:
  display_name: "Invariant Security Review"
  short_description: "Structured invariant-driven code security scans"
  default_prompt: "Use $invariant-security-review to build a threat model and run an evidence-backed security review of this repository."
policy:
  allow_implicit_invocation: true
